# Taste
- Prefers the assistant to review the entire project/codebase before proposing any changes or expansions ("please go through the complete project... I want to expand this further"). Confidence: 0.8
- Wants the plan explained to them before any implementation begins ("first explain me what is the plan") — expects a written plan/summary to review, not immediate execution. Confidence: 0.9
- Wants explicit approval before the assistant makes any changes; expects the assistant to pause and wait for a go-ahead after presenting the plan (interrupts execution and instructs holding off on all changes until approval; explicitly asks to stay in plan mode: "do not change anything without informing, be on plan mode"; restates it when giving new tasks: "Do not modify any files"; for the rule-engine review he again scoped the task to analysis only: "Do NOT modify any files yet" and "Do not modify the rule engine yet"). Confidence: 0.98
- Prefers the app under test to be inspected in a live browser via Playwright/browser automation (open the page and inspect it) rather than guessing selectors from code — asked the assistant to "open the application under test in a browser using Playwright and inspect the login page," and repeated this request across sessions. Had at one point insisted this be done through the connected Playwright **MCP server** specifically, forbidding ad-hoc Node.js scripts or `@playwright/test` as a fallback, but later told the assistant to drop that MCP-integration requirement ("forgot about mcp integration what i have discussed before in this chat") and just continue the task — so the durable preference is live browser inspection, not a specific tooling path. Confidence: 0.75
- When a required tool/prerequisite (e.g., the Playwright MCP server) is not connected/available, prefers the assistant to stop and clearly report that it's unavailable rather than improvise a workaround — "If Playwright MCP is unavailable, stop and report that MCP is not connected." Also appreciates being given the exact command to fix it (e.g., `cmd mcp add playwright -- npx -y @playwright/mcp@latest`). Confidence: 0.85
- Prefers to implement changes himself, step by step: asks for guidance in incremental steps ("tell me step by step, i will impliment one by one") and writes the code/tests himself, using the assistant for teaching, review, and diagnosis rather than full execution. Fresh evidence: even when deleting files, he does the removal himself ("i am deleting... i will remove one by one, in index files as well") and wants only the impact analysis and checklist from the assistant. Fresh evidence again: for the approved barrel fix he had already made the code change himself ("i already done most of them") and wanted the assistant only to verify and report. Confidence: 0.9
- Before deleting/removing any code, wants the assistant to verify every reference first — a grep-based impact analysis covering imports, index.ts barrel exports, fixtures, and test files — and confirm explicitly that nothing else will break, plus a step-by-step deletion checklist (e.g., "can you please confirm, if i delete loginpage2.ts loginmodule2.ts and homepage, nothing will impact in my project"). Confidence: 0.8
- When hitting errors, asks "why" and wants the root cause explained (e.g., diagnosis of a `String` vs `string` type mismatch, or a module-resolution/import error), not just a silent fix. Confidence: 0.8
- Keeps project documentation in sync with the code: maintains README.md plus a "complete framework understanding" docx and asks for both to be updated to reflect the latest changes after code/work is done. Confidence: 0.7
- Prefers to manage tooling configuration (MCP servers, agent definitions) through GitHub/repository-level config rather than local machine setup — stated "we will configure mcp and agents by using github," i.e., version-controlled, team-shared, and reusable in GitHub Actions/devcontainers. Fresh evidence: when offered project vs local vs user scope for the Playwright MCP server, he approved the project-scope command (`--scope project`, which writes `.mcp.json` into the repo root for git), not a local/user-private setup. Confidence: 0.85
- When asking the assistant to read/fetch project content, wants exhaustive, complete output in one go — the entire folder structure (every folder and file name) and full contents of all files, not summaries or partial reads. Repeated request pattern: "i want to copy each folder ... all folders and all content in one go, fetch everything under this and give me" and "give me the content which are under this folder, in detailed way so that i will copy" (he wants verbatim content to copy elsewhere). Confidence: 0.8
- Communicates errors from his own in-progress code via IDE context snippets (file path + line numbers + selected code) and expects the assistant to pinpoint the exact problem in those lines and explain the root cause before/while fixing (e.g., `String` vs `string` type mismatch, missing fixture implementation, filename-casing import issues). Confidence: 0.6
- Before destructive changes (deleting files / cleaning references), wants a written backup/restore note saved in the repo — full contents of every removed file plus the exact edits made (e.g., `docs/DELETED-FILES-BACKUP.md`) — so everything can be reverted later if issues arise ("make a note of it, in future if i want i will ask you to revert it, if i face any issue"). Confidence: 0.85
- Keeps code for separate projects in the same repository and explicitly marks those files off-limits during cleanups/refactors ("signin and base page is created for separate project, do not touch"); expects the assistant to leave them untouched and report which files were intentionally preserved. Fresh evidence: in the rule-engine cleanup he again enumerated a keep-list — "Keep: BasePage.ts, SignInPage.ts, SignInModule.ts, InventoryPage.ts, scripts/rule-engine.js, rules/framework-rule-engine.json" — and forbade modifying them. He calls these "practice files which I have implemented" and wants them excluded from the project's automated validation (rule engine) so they are not flagged as violations — "ignore these three files, don't consider these" — rather than being fixed or deleted. Confidence: 0.85
- After making changes/deletions, wants a prescribed verification sequence run and the results reported: type-check (`tsc --noEmit`) plus the project's rule-engine check (`npm run rules:check`), then a final summary of the pass/fail state (e.g., "Run tsc --noEmit. Run npm run rules:check. Report the final results."). Confidence: 0.7
- Once the user has decided which rule-engine violations are intentional/acceptable (legit exceptions like abstract BasePage, or false positives), expects the assistant to leave them untouched — no fix attempts and no rule-engine modifications ("Do not attempt to fix the remaining BasePage or InventoryPage rule violations. Do not modify the rule engine.") — and simply report the remaining state. This extends to tooling/config generally: when the assistant proposed adding an `excludeFiles` mechanism to the rule engine to skip his practice files, he rejected it outright ("no dont add in rule engine") and chose to delete the files himself instead. Fresh evidence: for the approved barrel fix he again ordered "Do NOT fix unrelated warnings or known rule violations" and expected the final report to list remaining pre-existing issues without touching them. Fresh evidence: for the documentation task he again ordered "Do NOT fix the remaining rule-engine warning/error" and wanted the existing findings documented as intentionally untouched. Confidence: 0.9
- Enforces a strict 3-layer test architecture — Tests → Modules → Pages — with explicit ownership rules: tests must not use Page Objects directly, Modules must not contain direct locators, and Page Objects own locators and basic UI actions. Stated as hard architecture rules when defining the Healer agent ("Do not bypass the existing framework architecture."). Confidence: 0.8
- Wants fixes to be minimal and surgical: prefers the smallest possible fix, does not want working tests rewritten unnecessarily, test intent changed, or the approved scenario scope expanded; if a fix cannot be made within these constraints, the agent must stop and report rather than force it. Confidence: 0.8
- Wants the fix workflow strictly ordered as "Observe first. Diagnose second. Fix third. Verify last." — reproduce/collect evidence and root-cause the failure before touching any code, then verify after the fix; explicitly called out as the "Most important rule" for the Healer. Confidence: 0.85
- Restricts each task to a strictly enumerated file list — "Create/update ONLY: ..." followed by an explicit do-not-modify list (other agent files, TS sources, tests, pages, modules, fixtures, rule-engine files, MCP config, docs) — and expects the assistant to touch nothing outside that scope (e.g., the Healer-agent task allowed only `.github/agents/healer.agent.md`). Confidence: 0.8
- Demands evidence-based diagnosis over guessing: "Never guess when evidence can be obtained" — reproduce the failure, gather repository and browser evidence, and never fabricate selectors, expected messages, or pretend browser evidence was collected when Playwright MCP is unavailable; if a safe fix cannot be determined from evidence, STOP and report the diagnosis rather than force one. Confidence: 0.8
- Wants agent definitions to be production-quality and structured: frontmatter (`name`/`description`/`tools`) using the existing planner.agent.md as a structural reference but with strictly non-overlapping roles (Healer ≠ Generator ≠ Planner — "You are NOT a test-generation agent"), explicit MUST NOT / MAY constraint lists, a diagnostic-classification scheme mapped to fix layers, and built-in human-approval gates for significant changes (assertions, intent, architecture, config). Confidence: 0.75
- Runs in a Windows PowerShell environment where generic CLI names are unavailable or collide (e.g., `mcp` is not on PATH, and `cmd` resolves to cmd.exe rather than Command Code's CLI). Expects the assistant to verify what binaries/commands actually exist in the environment (PATH checks, authoritative docs) before recommending any, and to give exact working commands — e.g., `command-code mcp add --scope project playwright -- npx -y @playwright/mcp@latest` instead of `cmd mcp add ...` — and explicitly forbids being asked to run commands that will fail in his shell. Confidence: 0.85
- After configuring a tool/MCP server, wants live verification that it is actually loaded and connected in the current session — not just that it is registered/enabled in config ("verify whether the current session has loaded/connected to the Playwright MCP server... confirm that Playwright browser tools are actually available in this current session"). Accepts evidence such as searching the tool registry for the exposed tools plus CLI status output, and expects the assistant to clearly distinguish config state from live connection state and explain how to get the tools loaded (new session or manual /mcp connect). Fresh evidence: he again requested verification in a standalone task — "Verify the project MCP configuration. Confirm that the Playwright MCP server from .mcp.json is loaded and connected in this session. Confirm that Playwright browser tools are available" — and the assistant satisfied it with a live functional call (`browser_tabs` list) in addition to reading `.mcp.json`, which he accepted as complete verification. Confidence: 0.8
- For diagnostic/healing exercises, wants the failure classified into a fixed taxonomy — locator issue, page-object issue, module/workflow issue, test/assertion issue, or application defect — mapped to the owning layer (Tests → Modules → Pages), and the output delivered as a structured written report with the exact sections: Failure, Evidence, Root cause, Affected layer, Recommended file, Proposed minimal fix, and why the fix belongs in that layer. The minimal fix must be explained but explicitly NOT implemented (read-only exercise: "Do NOT modify any files. Do NOT fix anything."). Confidence: 0.85
- When asked to convert pasted/plain-text content into a proper agent definition file (e.g., generator.agent.md), wants properly formatted markdown (headings, closed code fences, frontmatter) whose examples are grounded in the actual framework code — real method names (`dologin` not `doLogin`), the framework's lazy arrow-function locator pattern, and npm scripts verified against package.json — with stale references to deleted files removed, and wants the three agent files (Planner/Generator/Healer) kept mutually consistent (matching frontmatter/section conventions) with their responsibilities confirmed non-overlapping ("make sure three agent files together and confirm that Planner, Generator, and Healer responsibilities do not overlap"). Confidence: 0.7
- When documenting completed work, prefers updating an existing architecture/agent doc rather than creating a new or duplicate file — explicitly told the assistant to "Prefer updating an existing Healer/Planner/Generator documentation file rather than creating a duplicate document" — and wants all existing content preserved with only the required additions appended. Confidence: 0.85
- Wants documented project decisions to include explicit guardrail and constraint lists (e.g., "Never rewrite passing tests. Never weaken assertions... Always make the smallest possible code change. Always verify with TypeScript + rule check + original failing test. If the failure cannot be safely fixed within the architecture, stop and report.") recorded verbatim in the docs, so the documented behavior of the system matches what the agents are instructed to do. Confidence: 0.7
- After a scoped change, verifies scope compliance with git evidence, not just the assistant's assertion: asks which file was modified, `git diff --stat`, the full `git diff` of the changed file(s), and an explicit confirmation that no source-code files were touched outside the approved scope (e.g., after the documentation-only task he asked "Which documentation file was modified / git diff --stat / git diff for the documentation file / Confirm that no source-code files were modified"). Confidence: 0.65
irmed non-overlapping ("make sure three agent files together and confirm that Planner, Generator, and Healer responsibilities do not overlap"). Confidence: 0.7
 Planner, Generator, and Healer responsibilities do not overlap"). Confidence: 0.7
m that Planner, Generator, and Healer responsibilities do not overlap"). Confidence: 0.7
